apiVersion: v1
kind: Service
metadata:
  name: rag-api-service
  labels:
    app: rag-api
spec:
  selector:
    app: rag-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP